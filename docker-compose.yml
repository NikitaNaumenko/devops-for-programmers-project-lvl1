version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.production
    command: make test
    env_file: ./.env.example
    depends_on:
      - db

  db:
    env_file: ./.env.example
    image: postgres:11
